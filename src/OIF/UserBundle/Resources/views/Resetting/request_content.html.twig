{% trans_default_domain 'FOSUserBundle' %}

<div class="panel panel-info">
    <div class="panel-heading">
        <h2 class="panel-title">Réinitialisation du mot de passe</h2>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-lg-12">
                <div class="col-md-6 col-md-offset-2 text-center">
                    <h5>Si vous avez oublié votre mot de passe, vous pouvez le réinitialiser en renseignant votre adresse électronique ou identifiant dans le formulaire ci-dessous.</h5>
                    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
                        <div class="form-group">
                            <input class="form-control" type="text" id="username" name="username" required="required" placeholder="{{ 'resetting.request.username'|trans }}" />
                        </div>
                        <div class="form-group">
                            <input class="btn btn-success pull-right" type="submit" value="{{ 'resetting.request.submit'|trans }}" />
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>

<hr>

